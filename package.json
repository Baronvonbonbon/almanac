{
  "name": "almanac-workspace",
  "private": true,
  "license": "GPL-3.0-or-later",
  "workspaces": [
    "probe",
    "app"
  ],
  "scripts": {
    "check": "npm run check -w app && npm run build -w probe",
    "whois": "node tools/whois.mjs"
  },
  "devDependencies": {
    "ethers": "^6.13.0"
  }
}
